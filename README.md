# CONVERSOR DE ARQUIVOS.PY EM EXECUTÁVEL

1. defina o "local_execução" (seu ambiente de criação de executáveis (venv)) onde esta o seu "arquivo.py"
2. selecione o arquivo.py dentro do "local_execução" definido
3. seu arquivo.py será convertido em um arquivo.exe dentro da pasta "dist"

## 📦 Requisitos

```bash
pip install subprocess,os,tkinter
```
